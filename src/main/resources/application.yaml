spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  datasource:
    url: jdbc:h2:tcp://localhost/~/springboot-aws
    username: sa
    password:
    driver-class-name: org.h2.Driver